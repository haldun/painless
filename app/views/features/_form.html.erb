<%= simple_form_for(@feature) do |f| %>
  <%= f.error_notification %>

  <div class="inputs">
    <%= f.association :project %>
    <%= f.input :name %>
  </div>

  <div class="actions">
    <%= f.button :submit %>
  </div>
<% end %>
